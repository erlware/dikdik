%% -*- erlang -*-
{application, dikdik,
 [{description, "Erlang interface for Heroku's PostgreSQL"},
  {vsn, "0.0.1"},
  {registered,[dikdik_sup]},
  {applications, [kernel
                 ,stdlib
                 ,sasl
                 ,lager
                 ,ssl
                 ,jsx
                 ,jesse
                 ,pgsql
                 ,poolboy]},
  {mod, {dikdik_app,[]}},
  {start_phases, []}]}.
